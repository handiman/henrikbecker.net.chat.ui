{
  "name": "cv-chat",
  "version": "1.0.0",
  "description": "Henrik Becker's CV chatbot as a web component",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --watch --serve"
  },
  "devDependencies": {
    "@stencil/core": "^4.0.0"
  }
}
